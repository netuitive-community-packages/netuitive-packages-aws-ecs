{
  "analyticConfiguration": {
    "metrics": [
      {
        "match": "^aws\\.ecs\\.(cpu.*|memory.*)",
        "properties": {
          "tags": {
            "unit": "percent"
          },
          "validMax": 100
        }
      }
    ],
    "name": "AWS ECS Cluster",
    "scope": {
      "elementType": "ECS Cluster"
    },
    "type": "METRIC_META"
  }
}